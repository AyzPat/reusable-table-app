"use strict";
var page_1 = require('./page');
var PageBuilder = (function () {
    function PageBuilder() {
    }
    PageBuilder.prototype.getPages = function (visiblePages, activePage, pageCount) {
        var pages = [];
        if (1 < visiblePages) {
            var minPage = activePage - visiblePages;
            var maxPage = activePage + visiblePages;
            for (var pageValue = minPage; pageValue <= maxPage; ++pageValue) {
                if (0 < pageValue && pageValue <= pageCount) {
                    pages.push(new page_1.Page(pageValue));
                }
            }
        }
        else {
            for (var pageValue = 1; pageValue <= pageCount; ++pageValue) {
                pages.push(new page_1.Page(pageValue));
            }
        }
        return pages;
    };
    return PageBuilder;
}());
exports.PageBuilder = PageBuilder;
//# sourceMappingURL=pageBuilder.js.map